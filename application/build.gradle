apply plugin: 'application'

dependencies {
    compile project(':core')
}

sourceSets {
    main {
        resources {
            srcDirs += [
                project(':core').sourceSets.main.resources
            ]
        }
    }
}

mainClassName = "${project.group}.Touka"